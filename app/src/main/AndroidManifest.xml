<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicalstructureapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
            <activity
            android:name=".MusicGenresActivity"
            android:label="@string/music_genres" />
        -->
        <activity
            android:name=".WinterWonderlandActivity"
            android:label="@string/genre_winter_wonderland" />
        <activity
            android:name=".UkeleleChristmasActivity"
            android:label="@string/genre_ukelele_christmas" />
        <activity
            android:name=".KidsStuffActivity"
            android:label="@string/genre_kids_stuff" />
    </application>

</manifest>